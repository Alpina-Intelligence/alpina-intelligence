"""Bake-off harness: runs candidate models against a fixture set and produces
a comparison report.

Filled in next session. For now this module is just a placeholder so the
package imports cleanly.
"""

# TODO: load fixtures from fixtures/passages.jsonl (with hand labels)
# TODO: for each model in CANDIDATES, call get_client(...) and annotate each fixture
# TODO: score agreement against hand labels, time generation, write a report
